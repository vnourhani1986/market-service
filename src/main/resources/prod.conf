akka {
  loglevel = INFO
  stdout-loglevel = INFO
  loggers = ["akka.event.slf4j.Slf4jLogger"]
  logger-startup-timeout = 30s
  default-dispatcher {
    fork-join-executor {
      parallelism-min = 8
    }
  }
  test {
    timefactor = 1
  }
  http {
    server {
      server-header = "myticket.snapptrip.com REST API"
      //request-timeout = 30s
    }
  }
}


db {
  url = "jdbc:postgresql://192.168.1.14:5432/master?ApplicationName=MARKET"
  user = "pgsql"
  password = "Astronomiex1235"
  poolName = "master"
  numThreads = 10
  connectionTimeout = 10s
  validationTimeout = 10s
  properties {
    sslfactory = org.postgresql.ssl.NonValidatingFactory
  }
}

slick-postgres {

  profile = "slick.jdbc.PostgresProfile$"

  db {
    dataSourceClass = "slick.jdbc.DriverDataSource"
    properties = {
      driver = "org.postgresql.Driver"
      url = "jdbc:postgresql://192.168.1.14:5432/flight?ApplicationName=Flight&sslfactory=org.postgresql.ssl.NonValidatingFactory"
      user = "pgsql"
      password = "Astronomiex1235"
    }

  }
}

http {
  host = "0.0.0.0"
  port = 9000
}

https {
  host = "0.0.0.0"
  port = 9000
}

snapptrip-auth {
  api-base-url = "auth.core"
  login-url = "/v1/auth"
  auth-url = "/v1/auth"
  port = 9000
}

web-engage {
  host = "api.webengage.com"
  api-base-url = "/v1/accounts/"
  event-url = "/events"
  user-url = "/users"
  license-code = "11b5650c0"
  api-key = "Bearer 9f86b2a7-1497-4adc-856b-df4211abb0cc"
}


sentry {
  dns: "http://05fd30058bef4c148c747afdba90c832@sentry.snapptrip.com/25"
  environment: "production"
}

// This is used when calculating datetimes, durations, intervals, and FlightModule.now().
timezone = "Asia/Tehran"

redis {
  ip: "192.168.1.3"
  db: 5
  connectionTimeout: 30s
}